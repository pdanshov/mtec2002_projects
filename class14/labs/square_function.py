"""
square_function.py
===
1. Wrap your previous square script in a function.  Pass in name, x and y position, and z.
2. Set the location by doing this:
    obj = bpy.data.objects.new(name + '_obj', mesh)
    obj.location.x = x
    obj.location.y = y
3. Use the z value when defining your vertices.

"""
